# datasets
# ucf24 (action tubes from a pkl database)
    # sequences
    # 2d only
# progress images
    # sequences
    # 2d only
# progress text
    # sequences
    # 2d only

# can a wrapper be made to turn sequences into 2d? 
    # probably not because sequences would load all images when we only need 1
# can progress images and text be combined?